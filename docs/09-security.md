# Security

*Status: Not started.*

---

## Planned content

- Authority level enforcement (L1/L2/L3 policy, where it is enforced in the graph)
- Secret Manager integration (which secrets, how referenced in Cloud Run)
- Service account design (least-privilege IAM for each Cloud Run service)
- FastAPI authentication (approval endpoint security)
- Audit logging (what actions are logged, where)
- Data isolation (per-investigation state isolation, no cross-incident leakage)
